for (let x = 1;  x <= 6; x++) { // first is the iterator initialised, next is the condition like running n times, then is the operation for increment for our loop or cycle
    console.log(x);
}

for (let x = 0; x < 6; x++){
    if (x % 2 === 0){
        console.log(x);
    }
}